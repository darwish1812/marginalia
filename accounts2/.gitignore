.claude/
index.test.html
index.auto.test.html
supabase/.temp/

# secrets for a locally served function — never committed
.env
.env.*
supabase/functions/.env*

# the test harness — the app itself still has no build step
node_modules/
index.local.html
test-results/
playwright-report/
